name: Legacy Windows Installer Diagnostic

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: echo "Automatic installer validation moved to Windows Installer Deterministic. This legacy workflow is manual-only and retained only for historical diagnostics."
