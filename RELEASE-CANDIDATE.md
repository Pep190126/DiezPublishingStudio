# Diez Publishing Studio — current preview candidate

## 0.8.2-preview

- Editable Master remains non-destructive for imported source materials.
- Edition Freeze is exposed in the desktop UI.
- Preflight is exposed in the desktop UI with blocking checks and warnings.
- Edition Freeze snapshots are persisted in the `.diez` package.
- Windows installer pipeline runs package, graph, Bible, consistency, revision, editable-master, freeze and preflight self-tests.
